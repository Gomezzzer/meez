<%= form_with(model: meez_en_place) do |form| %>
  <% if meez_en_place.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meez_en_place.errors.count, "error") %> prohibited this meez_en_place from being saved:</h2>

      <ul>
        <% meez_en_place.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ingredient_name, style: "display: block" %>
    <%= form.text_field :ingredient_name %>
  </div>

  <div>
    <%= form.label :measurement, style: "display: block" %>
    <%= form.text_field :measurement %>
  </div>

  <div class="form-group mt-4">
   <p class="card-title">Add Image</p>
    <%= form.file_field :image, class: "form-control-file" %>
    <div class="form-check">
      <%= form.check_box :remove_image, class: "form-check-input" %>
      <%= form.label :remove_image, "Remove Image", class: "form-check-label" %>
    </div>
  </div>

  <div class="form-group custom-button-row mt-2">
    <%= form.submit "Save", class: "custom-outline-btn" %>
  </div>
<% end %>
